import React, { useState } from "react";

// export default function Escena() {
//   const Linea = (props) => <p>{props.texto}</p>;
//   return Textos.map((text, index) => <Linea texto={text} />);
// }

// export default function Escena(props) {
//   const [activo, setActive] = useState(true);
//   const className = activo ? "enable" : "disabled";

//   return <p className={`p-escena ${className}`}>{props.texto}</p>;
// }
